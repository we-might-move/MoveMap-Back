-- =====================================================
-- B-Tree 인덱스 생성 (일반 방식)
-- =====================================================
-- 주의: 약 1-2분간 program 테이블 락 발생
-- 개발 환경에서만 사용 권장
-- =====================================================

CREATE INDEX IF NOT EXISTS idx_program_name_normalized_btree
    ON program(name_normalized);